scanf로 여러 줄의 값을 입력으로 받는 경우 버퍼에 값이 잘못 저장되는 경우를 어떻게 방지 가능한지.

int A, B;
scanf("%d %d",&A,&B);
int Arr[A][B];

이 코드에서 배열의 크기를 변수를 이용해 지정 안되기 때문에 정의할 수 없음 문제 발생.
출처: https://hublogmandoo.tistory.com/47 [심프슨정리:티스토리]

따라서 scanf로 입력받은 크기를 이용해서 배열을 선언하기 위해서는 동적할당을 사용할 수 있다.

자주사용하는 함수인 scanf()함수에 대한 이해가 필요하다.
여러 줄의 입력을 받아 수십개의 입력을 여러 줄로 받아야 하는 상황에서 함수에 대한 이해가 부족할 경우
제대로 원하는 입력을 받아서 저장시키는게 불가능하다. 따라서 

제출시간 조건에 대한 이해가 필요하다.
이를 위해서 빅오 표시법으로 연산 입력 계수와 반복 수 파악해서 연산 시간을 계산할 수 있어야 한다.
https://lemonlemon.tistory.com/54


내가 목표 정했는데, 우선 42 seoul 라피신 준비 과정과 더불어, 선린고 코딩 문제 풀어보고 제대로 졸업을 갖자.
